# Local development: MongoDB only.
# Run from backend root: docker compose up -d
# Backend connects with MONGO_URI=mongodb://localhost:27017/damplab (default).
version: '3'
services:
  mongo:
    image: mongo:7
    ports: ["27018:27017"]
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
