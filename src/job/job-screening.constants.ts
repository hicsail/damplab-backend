/** Catalog `name` values from `scripts/src/assets/services.json` (JSON `id` is not stored in Mongo). */
export const GIBSON_ASSEMBLY_SERVICE_NAME = 'Gibson Assembly';
export const M_CLONING_SERVICE_NAME = 'Modular Cloning';
